version: "3.8"
services:
  glossary:
    build: .
    ports:
      - "8000:8000"   # FastAPI
      - "50051:50051" # gRPC
    volumes:
      - ./glossary.db:/app/glossary.db
    environment:
      - DATABASE_URL=sqlite:///./glossary.db
      - HTTP_PORT=8000
      - GRPC_PORT=50051
